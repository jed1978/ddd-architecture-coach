# Architecture State

> Maintained by the DDD Architecture Coach. Tracks **current focus only** — phase completion status, output documents, and per-BC summaries are derivable from `{coach_output_root}/` (and from `spec.md` frontmatter for Phase 3 stability) and are NOT stored here.
>
> Phase 4 review history lives in `.claude/arch-learnings.md` under `phase_4_reviews:`.
> Cross-phase open questions and learnings live in `.claude/arch-learnings.md` under `learnings:` / `open_questions:`.
>
> See `SKILL.md` → State Determination for how the coach derives phase progress.
>
> Users may edit manually; preserve `last_updated` when doing so.

---

```yaml
current_focus:
  bc:                    # BC name; blank when working at system level
  phase:                 # phase_1_step_1_4 | phase_1_step_5_6 | phase_2 | phase_3 | phase_4

last_updated:            # ISO date, e.g., 2026-05-04
```
